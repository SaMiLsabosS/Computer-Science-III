
public class Notes
{
    // HashCode: Object --> int
    // You have to implent hashcoding to create a unique Integers for eachobject name od your choice
    // Load Factor: What point does it rehash. Ideal is O(4), a percentage
    // Java's is 75% load factor
    // collision is when something is hashcoded to the same number
    // hashmap fixes that for you 
    // How to hashcode a String: s[0]*31^(n-1) + s[1]*31^(n-2) + ... + s[n-1]
}
