
public class Notes
{
    public static void main(){
        //Interface: specification of something. That something can be implentmented in different ways
        //get(Object key) //Returns the value to which the specified key is mapped, or null if this map contains no mapping for the key
        //put(K key, V value) // V is the variable type //Associates the specified value with the specified key in this map
        //keySet() //Returns a Set view of the keys contained in this map
    }
}
